<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowTitleSize">40dp</item>
        <item name="android:actionBarStyle">@style/my_actionbar_style</item>
        <item name="android:windowTitleBackgroundStyle">@style/CustomWindowTitleBackground</item>
        <item name="vpiTabPageIndicatorStyle">@style/CustomTabPageIndicator</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <!-- 白底的应用启动页面 -->
    <style name="Theme.AppStartLoadTranslucent" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>
    <!-- 白底的应用启动页面 -->
    <style name="Theme.StartAppTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>
    <!-- 等待对话框的dialog -->
    <style name="progress_dialog" parent="@android:style/Theme.Dialog">
        <!-- dialog的windowfragme是否存在边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 是否浮在activity 之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 是否现实dialog title -->
        <item name="android:windowNoTitle">true</item>
        <!-- 设置dialog 背景 -->
        <item name="android:background">@null</item>
        <item name="android:windowBackground">@null</item>
        <!-- 背景是否模糊显示 -->
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="navigation_bottom_radio">

        <!-- 内部组件的排列 -->

        <item name="android:gravity">center</item>

        <!-- 背景样式 -->
        <!-- <item name="android:background">@drawable/radiobuttonbackground</item> -->
        <!-- 宽度 -->

        <item name="android:layout_width">fill_parent</item>

        <!-- 高度 -->

        <item name="android:layout_height">wrap_content</item>

        <!-- 设置RadioButton的原来图片为空 -->

        <item name="android:button">@null</item>

        <!-- 与其他组件宽度占相同比重 -->

        <item name="android:layout_weight">1.0</item>

        <!-- 底部的空隙 -->

        <item name="android:paddingBottom">2.0dip</item>

        <!-- 顶部的空隙 -->

        <item name="android:paddingTop">8.0dip</item>

        <!-- 文字的大小 -->

        <item name="android:textSize">11dip</item>

        <!-- 文字的颜色 -->

        <item name="android:textColor">@drawable/blue_radiobutton_text_selector</item>
    </style>

    <style name="exchange_radiobutton_style">

        <!-- 内部组件的排列 -->

        <item name="android:gravity">center</item>

        <!-- 背景样式 -->
        <!-- <item name="android:background">@drawable/radiobuttonbackground</item> -->
        <!-- 宽度 -->

        <item name="android:layout_width">fill_parent</item>

        <!-- 高度 -->

        <item name="android:layout_height">wrap_content</item>

        <!-- 设置RadioButton的原来图片为空 -->

        <item name="android:button">@null</item>

        <!-- 与其他组件宽度占相同比重 -->

        <item name="android:layout_weight">1.0</item>

        <!-- 底部的空隙 -->

        <item name="android:paddingBottom">8.0dip</item>

        <!-- 顶部的空隙 -->

        <item name="android:paddingTop">8.0dip</item>

        <!-- 文字的大小 -->

        <item name="android:textSize">11dip</item>

        <!-- 文字的颜色 -->

        <item name="android:textColor">@drawable/blue_radiobutton_text_selector</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:layout_marginRight">15dp</item>
        <item name="android:layout_marginLeft">15dp</item>

    </style>
    <style name="marquee_style">
        <item name="android:marqueeRepeatLimit">marquee_forever</item>
        <item name="android:singleLine">true</item>
        <item name="android:scrollHorizontally">true</item>
        <item name="android:focusableInTouchMode">true</item>
        <item name="android:focusable">true</item>
    </style>
    <style name="menu_item_style">
        <item name="android:gravity">center</item>
        <item name="android:layout_margin">10dp</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_height">wrap_content</item>
    </style>
    <!--主页的主菜单 父控件-->
    <style name="menu_container">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_margin">10dp</item>

    </style>
<!--主页菜单的 按钮 iamgeview -->
    <style name="menu_imageview">
        <item name="android:layout_width">50dp</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:scaleType">fitXY</item>
        <item name="android:layout_centerInParent">true</item>
    </style>
    <style name="menu_textview">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_marginTop">15dp</item>

    </style>

    <style name="my_actionbar_style" parent="@android:style/Widget.Holo.Light.ActionBar">
        <!-- <item name="android:background">#87CEFA</item> -->
        <item name="android:titleTextStyle">@style/actionbar_style</item>
    </style>
    <style name="actionbar_style" parent="@android:style/TextAppearance.Holo.Large">
        <item name="android:textColor">#424242</item>
        <item name="android:textSize">15sp</item>
    </style>
    <style name="CustomWindowTitleBackground">
        <item name="android:background">@color/red</item>
        <item name="android:textColor">@android:color/background_light</item>
    </style>
    <style name="CustomTabPageIndicator" parent="Widget.TabPageIndicator">
        <item name="android:background">@drawable/viewpager_background</item>
        <item name="android:textAppearance">@style/CustomTabPageIndicator.Text</item>
        <item name="android:textColor">@drawable/blue_title_textcolor</item>
        <item name="android:textSize">16sp</item>
        <!-- <item name="android:divider">@drawable/custom_tab_indicator_divider</item> -->
        <item name="android:paddingLeft">8dp</item>
        <item name="android:paddingRight">8dp</item>
        <item name="android:fadingEdge">horizontal</item>
        <item name="android:fadingEdgeLength">8dp</item>
    </style>
    <style name="CustomTabPageIndicator.Text" parent="android:TextAppearance.Medium">
        <item name="android:typeface">monospace</item>
    </style>
    <!--兑换风格的-->
    <style name="exchange_textview_style">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@drawable/textview_style</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">18sp</item>
        <item name="android:layout_marginLeft">20dp</item>
        <item name="android:layout_marginRight">20dp</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:layout_marginTop">10dp</item>

    </style>
    <style name="personalfile_textview_style">
        <item name="android:textSize">15sp</item>
    </style>
</resources>
